#!/bin/bash

# Script to make a bunch of spec files for the F datasets.
# See p38 of the cp3-bench 2025 paper.

declare -a data_sets
data_sets=( f1{a,b} )
data_set_dir=datasets
data_set_ext=.csv
run_key=2026-06-23-1045
super_spec=specialized-super-spec.toml
source ../Make-specs
