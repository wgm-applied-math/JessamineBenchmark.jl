#!/bin/bash

# Script to make a bunch of spec files

declare -a data_sets
data_set_dir=ode-strogatz
data_set_ext=.txt
run_key=2026-06-20-1900

super_spec=polynomial-super-spec.toml
data_sets=( d_{lv,vdp}{1,2} )
source ../Make-specs

super_spec=rational-function-super-spec.toml
data_sets=( d_{bacres,predprey}{1,2} )
source ../Make-specs

super_spec=hard-super-spec.toml
data_sets=( d_{barmag,glider,sfgrid,shearflow}{1,2} )
source ../Make-specs
